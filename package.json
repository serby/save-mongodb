{
  "author": "Paul Serby <paul@serby.net>",
  "name": "save-mongodb",
  "description": "mongodb persistence engine for save",
  "version": "1.0.4",
  "tags": [
    "database",
    "save",
    "persistence"
  ],
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:serby/save-mongodb"
  },
  "license": "ISC",
  "main": "lib/mongodb-engine",
  "scripts": {
    "lint": "eslint -f unix .",
    "inspect": "jsinspect",
    "pretest": "npm run lint && npm run inspect",
    "test": "istanbul cover ./node_modules/.bin/_mocha test",
    "posttest": "(istanbul check-coverage --statements 85 --branches 80 --functions 90 --lines 90 && rm -rf coverage) || echo Look at 'coverage/lcov-report/index.html' to find out more"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "dependencies": {
    "lodash": "^4.3.0",
    "mongodb": "^1.4.40",
    "through2": "^2.0.1"
  },
  "devDependencies": {
    "async": "^1.5.2",
    "eslint": "^1.4.1",
    "eslint-config-clock": "^1.0.0",
    "eslint-config-standard": "^4.3.2",
    "eslint-plugin-standard": "^1.3.0",
    "event-stream": "^3.3.2",
    "istanbul": "^0.3.19",
    "jsinspect": "^0.7.0",
    "mocha": "^2.3.2",
    "save": "^2.1.2",
    "stream-assert": "^2.0.3"
  }
}
